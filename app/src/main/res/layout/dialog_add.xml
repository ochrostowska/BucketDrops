<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/str_add_drop"
        android:layout_marginTop="@dimen/title_tv_vertical_margin"
        android:layout_marginBottom="@dimen/title_tv_vertical_margin"
        android:textSize="@dimen/title_tv_font_size"/>
        
    <ImageButton
        android:id="@+id/closeButton"
        android:layout_width="18sp"
        android:layout_height="18sp"
        android:background="@color/transparent"
        android:src="@drawable/ic_close"
        android:layout_alignTop="@+id/titleTextView"
        android:layout_alignParentRight="true" />

    <EditText
        android:id="@+id/dropEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/titleTextView"
        android:singleLine="true"
        android:hint="@string/hint_aad"
        android:padding="16dp"/>

    <DatePicker
        android:id="@+id/datePicker"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@+id/dropEditText"/>

    <Button
        android:id="@+id/addItButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/datePicker"
        android:text="@string/str_add_it"
        android:textAllCaps="false"

        />
</RelativeLayout>